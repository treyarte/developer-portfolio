/**
 * Timeline item position for either the left or right side of the line.
 */
export enum TimelinePosEnum {
    //We are starting are 1 because in js 0 is a falsy value and can cause some issues
    left = 1,
    right = 2
}